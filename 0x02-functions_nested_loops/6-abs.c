#include "main.h"

/**
 * _abs - This computes the absolute value of an integer
 * @n: This is the int to check
 * Return: This returnsthe absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

